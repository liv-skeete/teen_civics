alembic==1.13.3
bcrypt==4.2.1
requests==2.32.3
tweepy>=4.15.0
python-dotenv==1.0.0
feedparser==6.0.10
openai>=1.0.0
httpx<0.28
Flask==3.0.0
Flask-Limiter==3.5.0
Flask-WTF==1.2.1
Authlib==1.7.2
# Sign In with Apple — client_secret is an ES256 JWT signed from the .p8,
# regenerated every ~6 months. PyJWT handles signing; cryptography
# provides the EC backend. Authlib used to pull both in transitively;
# newer versions ship joserfc instead, so we depend on them explicitly.
PyJWT==2.10.1
cryptography==46.0.1
resend==2.30.1
beautifulsoup4==4.12.3
lxml==5.1.0
PyMuPDF>=1.25.1
pdfminer.six==20231228
regex==2024.5.15
psycopg2-binary==2.9.10
pytz==2024.1
gunicorn==23.0.0
cloudscraper>=1.2.71
atproto>=0.0.55